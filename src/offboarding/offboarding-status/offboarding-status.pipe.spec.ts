import { OffboardingStatusPipe } from './offboarding-status.pipe';

describe('OffboardingStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new OffboardingStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
