import { EquipmentStatusPipe } from './equipment-status.pipe';

describe('EquipmentStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
